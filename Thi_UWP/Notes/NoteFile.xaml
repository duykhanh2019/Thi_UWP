<Page
    x:Class="Thi_UWP.Notes.NoteFile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thi_UWP.Notes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" Margin="0 50 0 0"
                     MaxHeight="200" Width="400" Header="Notes"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" x:Name="Notes"/>
            <Grid HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="400"/>
                    <ColumnDefinition MaxWidth="400"/>
                </Grid.ColumnDefinitions>
                <Button Margin="0 20 0 0 ">Save</Button>
                <Button Margin="70 20 0 0" Click="ButtonRefresh_Onclick">Refresh</Button>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
